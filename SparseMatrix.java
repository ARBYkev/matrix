
public class SparseMatrix implements Node{

	void insert(int row, int column, int value);
    SparseMatrix transpose();
    SparseMatrix product(SparseMatrix sparseMatrix);
    void print();
	
	
}
